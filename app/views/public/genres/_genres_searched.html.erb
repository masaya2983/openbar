<% if user_signed_in? %>
  <div class="search_form">
    <%= form_with url:genre_path, local: true,method: :get do |f| %>
     <%= f.collection_select :genre, Category.all, :id, :name %>
      <%= f.submit "ジャンル検索" %>
      <% end %>
  </div>
<% end %>