<table>
  <tbody>
    <% liquor.comments.each do |comment| %>
    <tr>
      <td>
        <%= link_to user_path(comment.user)do %>
         <%= image_tag comment.user.get_profile_image, size: "50×50"%>
         <%= comment.user.name%>
         <% end %>
      </td>
      <td><%= comment.comment %></td>
      <td>
        <% if comment.user == current_user %>
         <%= link_to "Destroy", comment, method: :delete, class: "btn btn-danger pull-right", remote: false %>
        <% end %>
      </td>
    </tr>
     <% end %>
  </tbody>
</table>